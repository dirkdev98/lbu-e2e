/* eslint-disable no-unused-vars */

// Generated by @lbu/code-gen at 2020-04-21T19:22:26.229Z
/* eslint-disable no-unused-vars */

export const validatorHooks = {};

const isNil = (value) => value === undefined || value === null;

/**
 * This function should not throw as the corresponding validator will do that
 * @callback ValidationErrorFn
 * @param {string} key
 * @param {Object} info
 */

/**
 * @type ValidationErrorFn
 */
let _errorFn = (key, info) => {
  const err = new Error(`ValidationError: ${key}`);
  err.key = key;
  err.info = info;

  return err;
};

/**
 * Set a different error function, for example AppError.validationError
 * @param {ValidationErrorFn} fn
 */
export function validatorSetErrorFn(fn) {
  _errorFn = fn;
}

export const todoValidators = {
  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoList}
   */
  list: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoList" in validatorHooks) {
      result = validatorHooks["preValidateTodoList"](result);
    }

    result = objectValidator0(result, propertyPath);

    if ("postValidateTodoList" in validatorHooks) {
      return validatorHooks["postValidateTodoList"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoItem}
   */
  item: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoItem" in validatorHooks) {
      result = validatorHooks["preValidateTodoItem"](result);
    }

    result = objectValidator4(result, propertyPath);

    if ("postValidateTodoItem" in validatorHooks) {
      return validatorHooks["postValidateTodoItem"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoCollection}
   */
  collection: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoCollection" in validatorHooks) {
      result = validatorHooks["preValidateTodoCollection"](result);
    }

    result = genericValidator7(result, propertyPath);

    if ("postValidateTodoCollection" in validatorHooks) {
      return validatorHooks["postValidateTodoCollection"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoAllResponse}
   */
  allResponse: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoAllResponse" in validatorHooks) {
      result = validatorHooks["preValidateTodoAllResponse"](result);
    }

    result = objectValidator10(result, propertyPath);

    if ("postValidateTodoAllResponse" in validatorHooks) {
      return validatorHooks["postValidateTodoAllResponse"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoNameParam}
   */
  nameParam: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoNameParam" in validatorHooks) {
      result = validatorHooks["preValidateTodoNameParam"](result);
    }

    result = objectValidator12(result, propertyPath);

    if ("postValidateTodoNameParam" in validatorHooks) {
      return validatorHooks["postValidateTodoNameParam"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoListResponse}
   */
  listResponse: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoListResponse" in validatorHooks) {
      result = validatorHooks["preValidateTodoListResponse"](result);
    }

    result = objectValidator14(result, propertyPath);

    if ("postValidateTodoListResponse" in validatorHooks) {
      return validatorHooks["postValidateTodoListResponse"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoNewBody}
   */
  newBody: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoNewBody" in validatorHooks) {
      result = validatorHooks["preValidateTodoNewBody"](result);
    }

    result = objectValidator16(result, propertyPath);

    if ("postValidateTodoNewBody" in validatorHooks) {
      return validatorHooks["postValidateTodoNewBody"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoCreateItemBody}
   */
  createItemBody: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoCreateItemBody" in validatorHooks) {
      result = validatorHooks["preValidateTodoCreateItemBody"](result);
    }

    result = objectValidator18(result, propertyPath);

    if ("postValidateTodoCreateItemBody" in validatorHooks) {
      return validatorHooks["postValidateTodoCreateItemBody"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoToggleItemBody}
   */
  toggleItemBody: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoToggleItemBody" in validatorHooks) {
      result = validatorHooks["preValidateTodoToggleItemBody"](result);
    }

    result = objectValidator20(result, propertyPath);

    if ("postValidateTodoToggleItemBody" in validatorHooks) {
      return validatorHooks["postValidateTodoToggleItemBody"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { TodoDeleteResponse}
   */
  deleteResponse: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateTodoDeleteResponse" in validatorHooks) {
      result = validatorHooks["preValidateTodoDeleteResponse"](result);
    }

    result = objectValidator22(result, propertyPath);

    if ("postValidateTodoDeleteResponse" in validatorHooks) {
      return validatorHooks["postValidateTodoDeleteResponse"](result);
    } else {
      return result;
    }
  },
};

export const unimplementedValidators = {
  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { UnimplementedUser}
   */
  user: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateUnimplementedUser" in validatorHooks) {
      result = validatorHooks["preValidateUnimplementedUser"](result);
    }

    result = objectValidator24(result, propertyPath);

    if ("postValidateUnimplementedUser" in validatorHooks) {
      return validatorHooks["postValidateUnimplementedUser"](result);
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { UnimplementedSettingsResponse}
   */
  settingsResponse: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateUnimplementedSettingsResponse" in validatorHooks) {
      result = validatorHooks["preValidateUnimplementedSettingsResponse"](
        result
      );
    }

    result = objectValidator29(result, propertyPath);

    if ("postValidateUnimplementedSettingsResponse" in validatorHooks) {
      return validatorHooks["postValidateUnimplementedSettingsResponse"](
        result
      );
    } else {
      return result;
    }
  },

  /**
   *
   * @param {*} value
   * @param {string} [propertyPath]
   * @returns { UnimplementedWindDirection}
   */
  windDirection: (value, propertyPath = "$") => {
    let result = value;

    if ("preValidateUnimplementedWindDirection" in validatorHooks) {
      result = validatorHooks["preValidateUnimplementedWindDirection"](result);
    }

    result = stringValidator41(result, propertyPath);

    if ("postValidateUnimplementedWindDirection" in validatorHooks) {
      return validatorHooks["postValidateUnimplementedWindDirection"](result);
    } else {
      return result;
    }
  },
};

export const lbuValidators = {};

function stringValidator1(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function referenceValidator3(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return todoValidators.item(value, propertyPath);
}

function arrayValidator2(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.array.undefined", { propertyPath });
  }

  if (!Array.isArray(value)) {
    throw _errorFn("validator.array.type", { propertyPath });
  }

  const result = [];

  for (let i = 0; i < value.length; ++i) {
    result.push(referenceValidator3(value[i], propertyPath + "[" + i + "]"));
  }

  return result;
}

function objectValidator0(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["name"] = stringValidator1(value["name"], propertyPath + "." + "name");

  result["items"] = arrayValidator2(
    value["items"],
    propertyPath + "." + "items"
  );

  return result;
}

function booleanValidator5(value, propertyPath) {
  if (isNil(value)) {
    return false;
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function stringValidator6(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function objectValidator4(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["completed"] = booleanValidator5(
    value["completed"],
    propertyPath + "." + "completed"
  );

  result["name"] = stringValidator6(value["name"], propertyPath + "." + "name");

  return result;
}

function stringValidator8(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function referenceValidator9(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return todoValidators.list(value, propertyPath);
}

function genericValidator7(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.generic.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.generic.type", { propertyPath });
  }

  const result = Object.create(null);
  for (const key of Object.keys(value)) {
    const validatedKey = stringValidator8(key, propertyPath + ".$key");
    const validatedValue = referenceValidator9(
      value[key],
      propertyPath + ".$value"
    );

    result[validatedKey] = validatedValue;
  }

  return result;
}

function referenceValidator11(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return todoValidators.collection(value, propertyPath);
}

function objectValidator10(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["store"] = referenceValidator11(
    value["store"],
    propertyPath + "." + "store"
  );

  return result;
}

function stringValidator13(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    value = String(value);
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  value = value.trim();

  if (value.length < 0) {
    const min = 0;
    throw _errorFn("validator.string.min", { propertyPath, min });
  }

  if (value.length > 30) {
    const max = 30;
    throw _errorFn("validator.string.max", { propertyPath, max });
  }

  return value;
}

function objectValidator12(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["name"] = stringValidator13(
    value["name"],
    propertyPath + "." + "name"
  );

  return result;
}

function referenceValidator15(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return todoValidators.list(value, propertyPath);
}

function objectValidator14(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["todo"] = referenceValidator15(
    value["todo"],
    propertyPath + "." + "todo"
  );

  return result;
}

function stringValidator17(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  value = value.trim();

  if (value.length < 1) {
    const min = 1;
    throw _errorFn("validator.string.min", { propertyPath, min });
  }

  if (value.length > 40) {
    const max = 40;
    throw _errorFn("validator.string.max", { propertyPath, max });
  }

  return value;
}

function objectValidator16(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["name"] = stringValidator17(
    value["name"],
    propertyPath + "." + "name"
  );

  return result;
}

function stringValidator19(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  value = value.trim();

  if (value.length < 1) {
    const min = 1;
    throw _errorFn("validator.string.min", { propertyPath, min });
  }

  if (value.length > 365) {
    const max = 365;
    throw _errorFn("validator.string.max", { propertyPath, max });
  }

  return value;
}

function objectValidator18(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["name"] = stringValidator19(
    value["name"],
    propertyPath + "." + "name"
  );

  return result;
}

function numberValidator21(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.number.undefined", { propertyPath });
  }

  if (typeof value !== "number") {
    value = Number(value);
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (!Number.isInteger(value)) {
    throw _errorFn("validator.number.integer", { propertyPath });
  }

  if (value < 0) {
    const min = 0;
    throw _errorFn("validator.number.min", { propertyPath, min });
  }

  return value;
}

function objectValidator20(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["index"] = numberValidator21(
    value["index"],
    propertyPath + "." + "index"
  );

  return result;
}

function booleanValidator23(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.boolean.undefined", { propertyPath });
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function objectValidator22(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["deleted"] = booleanValidator23(
    value["deleted"],
    propertyPath + "." + "deleted"
  );

  return result;
}

/**
 *
 */

function stringValidator26(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  value = value.trim();

  if (value.length < 36) {
    const min = 36;
    throw _errorFn("validator.string.min", { propertyPath, min });
  }

  if (value.length > 36) {
    const max = 36;
    throw _errorFn("validator.string.max", { propertyPath, max });
  }

  value = value.toLowerCase();

  if (
    !/^[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[a-f0-9]{4}-[a-f0-9]{12}$/gi.test(
      value
    )
  ) {
    throw _errorFn("validator.string.pattern", { propertyPath });
  }

  return value;
}

function uuidValidator25(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.uuid.undefined", { propertyPath });
  }

  return stringValidator26(value, propertyPath);
}

function stringValidator27(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.string.undefined", { propertyPath });
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function numberValidator28(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.number.undefined", { propertyPath });
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (!Number.isInteger(value)) {
    throw _errorFn("validator.number.integer", { propertyPath });
  }

  return value;
}

function objectValidator24(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["id"] = uuidValidator25(value["id"], propertyPath + "." + "id");

  result["name"] = stringValidator27(
    value["name"],
    propertyPath + "." + "name"
  );

  result["age"] = numberValidator28(value["age"], propertyPath + "." + "age");

  return result;
}

function booleanValidator30(value, propertyPath) {
  if (isNil(value)) {
    return true;
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function numberValidator31(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.number.undefined", { propertyPath });
  }

  if (typeof value !== "number") {
    value = Number(value);
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (!Number.isInteger(value)) {
    throw _errorFn("validator.number.integer", { propertyPath });
  }

  if (value < 0) {
    const min = 0;
    throw _errorFn("validator.number.min", { propertyPath, min });
  }

  if (value > 10) {
    const max = 10;
    throw _errorFn("validator.number.max", { propertyPath, max });
  }

  return value;
}

function referenceValidator32(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.reference.undefined", { propertyPath });
  }

  return unimplementedValidators.windDirection(value, propertyPath);
}

function numberValidator35(value, propertyPath) {
  if (isNil(value)) {
    return;
  }

  if (typeof value !== "number" || isNaN(value) || !isFinite(value)) {
    throw _errorFn("validator.number.type", { propertyPath });
  }

  if (value < 1) {
    const min = 1;
    throw _errorFn("validator.number.min", { propertyPath, min });
  }

  if (value > 150) {
    const max = 150;
    throw _errorFn("validator.number.max", { propertyPath, max });
  }

  return value;
}

function stringValidator36(value, propertyPath) {
  if (isNil(value)) {
    return new Date().toISOString();
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  return value;
}

function booleanValidator38(value, propertyPath) {
  if (isNil(value)) {
    return false;
  }

  if (typeof value !== "boolean") {
    if (value === "true" || value === 1) {
      value = true;
    } else if (value === "false" || value === 0) {
      value = false;
    }
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function arrayValidator37(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.array.undefined", { propertyPath });
  }

  if (!Array.isArray(value)) {
    throw _errorFn("validator.array.type", { propertyPath });
  }

  const result = [];

  for (let i = 0; i < value.length; ++i) {
    result.push(booleanValidator38(value[i], propertyPath + "[" + i + "]"));
  }

  return result;
}

function booleanValidator40(value, propertyPath) {
  if (isNil(value)) {
    return;
  }

  if (typeof value !== "boolean") {
    throw _errorFn("validator.boolean.type", { propertyPath });
  }

  return value;
}

function objectValidator39(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["foo"] = booleanValidator40(value["foo"], propertyPath + "." + "foo");

  return result;
}

function anyOfValidator34(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.anyOf.undefined", { propertyPath });
  }

  const errors = [];

  try {
    return numberValidator35(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  try {
    return stringValidator36(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  try {
    return arrayValidator37(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  try {
    return objectValidator39(value, propertyPath);
  } catch (e) {
    errors.push({
      name: e.name,
      message: e.message,
      key: e.key,
      info: e.info,
    });
  }

  throw _errorFn("validator.anyOf.type", { propertyPath, errors });
}

function arrayValidator33(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.array.undefined", { propertyPath });
  }

  if (!Array.isArray(value)) {
    throw _errorFn("validator.array.type", { propertyPath });
  }

  const result = [];

  for (let i = 0; i < value.length; ++i) {
    result.push(anyOfValidator34(value[i], propertyPath + "[" + i + "]"));
  }

  return result;
}

function objectValidator29(value, propertyPath) {
  if (isNil(value)) {
    throw _errorFn("validator.object.undefined", { propertyPath });
  }

  if (typeof value !== "object") {
    throw _errorFn("validator.object.type", { propertyPath });
  }

  const result = Object.create(null);

  result["darkMode"] = booleanValidator30(
    value["darkMode"],
    propertyPath + "." + "darkMode"
  );

  result["preferredNumber"] = numberValidator31(
    value["preferredNumber"],
    propertyPath + "." + "preferredNumber"
  );

  result["direction"] = referenceValidator32(
    value["direction"],
    propertyPath + "." + "direction"
  );

  result["totalMess"] = arrayValidator33(
    value["totalMess"],
    propertyPath + "." + "totalMess"
  );

  return result;
}

function stringValidator41(value, propertyPath) {
  if (isNil(value)) {
    return "NORTH";
  }

  if (typeof value !== "string") {
    throw _errorFn("validator.string.type", { propertyPath });
  }

  if (
    value !== "NORTH" &&
    value !== "EAST" &&
    value !== "SOUTH" &&
    value !== "WEST"
  ) {
    const oneOf = "NORTH, EAST, SOUTH, WEST";
    throw _errorFn("validator.string.oneOf", { propertyPath, oneOf });
  }

  return value;
}
